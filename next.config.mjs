/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        remotePatterns: [
            {
                hostname: "sensible-fly-873.convex.cloud",
            },
        ],
    },
};

export default nextConfig;
